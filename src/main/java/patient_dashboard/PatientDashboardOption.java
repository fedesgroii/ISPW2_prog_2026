package patient_dashboard;

/**
 * Represents the semantic choices available to a patient in the dashboard.
 * Used by Graphic Controllers to communicate user intent to the Application
 * Controller.
 */
public enum PatientDashboardOption {
    BOOK_VISIT,
    VISIT_HISTORY,
    SHOP,
    BULLETIN_BOARD,
    MANAGE_APPOINTMENTS,
    LOGOUT
}
